/**
 * 5 - Making Change - O(n) time / O(1) space
 * @param {Array of Ints} A - input array of denominations
 * @return {} - meeting times merged
 */
function makingChange(amount, A) {
  var res = 0;

  console.log("\t -> makingChange:", res);
  return res;
}

makingChange(4, [1, 2, 3]);
